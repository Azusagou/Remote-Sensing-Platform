# 服务器部署说明

## 一、前提条件

- 你有一台云服务器（如阿里云、腾讯云、华为云、AWS、Azure等），已开放所需端口（如80、443、3000、8000等）。
- 服务器已安装 Node.js、npm/yarn、Python、pip、nginx（如需反向代理）等环境。
- 你有服务器的SSH权限。

---

## 二、后端（FastAPI）部署

### 1. 代码上传

- 将后端代码（如 `api_server.py` 及相关依赖、模型文件、output目录等）上传到服务器指定目录。

### 2. 安装依赖

```bash
pip install fastapi uvicorn   # 已安装模型运行相关依赖，只为了启动后端服务，可以只安装这两个依赖
pip install -r requirements.txt # 从新环境安装所有依赖
```

### 3. 启动后端服务

```bash
uvicorn api_server:app --host 0.0.0.0 --port 8000 --reload
```
- `--host 0.0.0.0` 让外部可以访问
- `--port 8000` 可根据需要修改

### 4. 防火墙/安全组

- 确保服务器安全组或防火墙已开放8000端口（或你实际用的端口）。

---

## 三、前端（React）部署

### 1. 代码上传

- 将前端代码（如 `多模态遥感基础模型应用软件平台` 目录下的所有前端文件）上传到服务器指定目录。

### 2. 安装依赖

```bash
npm install

### 3. 构建前端静态文件

```bash
npm run build
```
- 构建后会生成 `build/` 目录（或 `dist/`，视你的脚手架而定）。

### 4. 部署静态文件

- 推荐用 nginx 或其它静态服务器托管 `build/` 目录。

#### nginx 配置示例

```nginx
server {
    listen 80;
    server_name your.domain.com;

    location / {
        root /your/project/path/build;
        try_files $uri $uri/ /index.html;
    }

    # 反向代理API请求到后端
    location /api/ {
        proxy_pass http://127.0.0.1:8000/api/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
    }

    # 如需下载图片/结果等静态文件
    location /output/ {
        alias /your/project/path/output/;
    }
}
```

- 修改 `your.domain.com` 和 `/your/project/path/` 为你的实际域名和路径。

---

## 四、前端代码需要修改的地方

### 1. API请求地址

- 本地开发时通常是 `http://localhost:8000`，部署到服务器后应改为服务器实际域名或IP。
- 推荐用**相对路径**（如 `/api/biomass_predict`），并用 nginx 反向代理到后端，这样前端代码无需改动。
- 如果不用nginx，需将所有 fetch/axios 请求的 `localhost` 改为服务器公网IP或域名。

**示例：**
```javascript
// 推荐
fetch('/api/biomass_predict', { ... })

// 如果不用nginx反代
fetch('http://your.server.ip:8000/api/biomass_predict', { ... })
```

### 2. 静态资源路径

- 图片、结果文件等静态资源路径要确保 nginx 或其它服务器能正确访问。
- 前端引用如 `/output/xxx.png`，nginx要配置好 `/output/` 路径。

---

## 五、后端代码需要修改的地方

- 检查所有文件路径、模型路径、输出路径，确保在服务器上有读写权限。
- 如有硬编码的 `localhost`、绝对路径等，需改为服务器实际路径或用相对路径。

---

## 六、常见问题与建议

1. **跨域问题**  
   - 用 nginx 反向代理可避免跨域。
   - 如不用nginx，FastAPI需配置 CORS（你已配置）。

2. **安全**  
   - 生产环境建议关闭 FastAPI 的 `--reload`，并用 gunicorn+uvicorn 或 supervisor/pm2 等守护进程管理。
   - 重要数据、模型文件注意备份。

3. **性能**  
   - 大文件上传、模型推理等建议用高性能服务器。
   - 可用 CDN 加速静态资源。

4. **域名与SSL**  
   - 建议绑定域名并配置SSL证书（https），可用 Let's Encrypt 免费证书。

---

## 七、部署流程小结

1. 上传前后端代码到服务器
2. 安装依赖，构建前端
3. 启动后端服务
4. 配置 nginx（或其它服务器）托管前端和反向代理API
5. 修改前端API请求为相对路径或服务器域名
6. 测试访问，确保功能正常

---

# 调试模式

### 由于在本地开发时，每次运行模型推理来返回数据所需时间过长，不利于及时调试，而上传数据到后端处理已测试正常运行，所以我添加了一个debug参数来直接返回 output 目录下已存在的测试结果文件路径或内容，只需要调整debug参数的值为true或false即可切换调试模式

- 文件相对路径：src/components/BiomassInversion.tsx
```typescript
formData.append('debug', 'true'); // 调试模式
```

---

# 需要改进的问题

## 1. 进度条显示

### 之前的进度条只是前端定时“模拟”进度，和后端实际处理进度无关。
  
### 如果你想让进度条**真实反映后端处理进度**，有以下几种常见方案：

---

## 方案一：后端分阶段返回进度（推荐）

### 1. 后端设计

- 后端处理任务时，将进度信息（如0~100%）保存到某个地方（如Redis、内存、数据库、文件等）。
- 提供一个“查询进度”的API接口，例如 `/api/predict_progress?task_id=xxx`，返回当前进度百分比。

### 2. 前端实现

- 前端发起图片上传和处理请求时，后端返回一个 `task_id`。
- 前端定时（如每500ms）轮询“进度查询”接口，实时更新进度条。
- 处理完成后，进度条到100%，显示结果。

**伪代码流程：**

```typescript
// 1. 提交任务
const formData = new FormData();
// ...append文件和参数...
const res = await fetch('/api/biomass_predict', { method: 'POST', body: formData });
const { task_id } = await res.json();

// 2. 轮询进度
const interval = setInterval(async () => {
  const progressRes = await fetch(`/api/predict_progress?task_id=${task_id}`);
  const { progress } = await progressRes.json(); // progress: 0~100
  setInversionProgress(progress);
  if (progress >= 100) {
    clearInterval(interval);
    // 获取最终结果
  }
}, 500);
```

---

## 方案二：后端处理用WebSocket实时推送进度

- 后端用WebSocket推送进度，前端用WebSocket接收并更新进度条。
- 适合大任务、需要实时反馈的场景，但实现复杂度略高。

---

## 方案三：仅上传文件时显示上传进度

- 如果你只关心“文件上传”进度，可以用 XMLHttpRequest 或 axios 的 `onUploadProgress`，显示上传进度。
- 但上传完成后，后端处理阶段还是需要轮询或WebSocket。

---

## 你当前的可行做法

### 1. 如果后端暂时没有进度API

- 可以在前端上传文件时，先用 `onUploadProgress` 显示上传进度。
- 上传完成后，显示“处理中...”的动画或不确定进度条，直到后端返回结果。

### 2. 如果后端可以实现进度API

- 按“方案一”实现，前端轮询进度接口，进度条和后端同步。

---

## 代码示例（仅上传进度）

如果你用 axios，可以这样：

```typescript
import axios from 'axios';

const startInversion = async () => {
  const formData = new FormData();
  // ...append文件...
  setInversionProgress(0);
  setIsInverting(true);

  await axios.post('/api/biomass_predict', formData, {
    onUploadProgress: (progressEvent) => {
      const percent = Math.round((progressEvent.loaded * 100) / progressEvent.total);
      setInversionProgress(percent);
    }
  });
  // 上传完成后，显示“处理中...”
  setInversionProgress(100);
  // 等待后端处理完成，获取结果
};
```

---

## 总结

- **最理想**：后端实现进度API，前端轮询或WebSocket实时同步进度。
- **仅上传进度**：用axios的onUploadProgress或XHR的progress事件。
- **后端处理阶段**：没有进度API时，只能显示“处理中...”动画。

---